# Rod v0.5.0 — Persy Storage Adapter Shipped (2026-07-22)

## Status
✅ MERGED to master (`8988471`) + tagged `v0.5.0` + pushed to Gitea.
✅ Five-clean-runs discipline: 5/5 pre-merge, 5/5 post-merge. 70/70 test executions green.

## What Shipped
- `src/adapters/persy_storage.rs` (652L) — Persy adapter, feature-gated via `--features persy`
- `tests/persy_e2e.rs` (5 e2e tests) — single-node Persy CRUD
- `tests/cross_backend_mesh_e2e.rs` (2 e2e tests) — 2 redb + 1 persy mesh convergence
- Flake fix (`c8e3547`):
  - `WsServer::peer_count()` accessor + `Clone` derive
  - `wait_for_peer_count()` test helper (50ms poll, 5000ms timeout)
  - Replaced both `sleep(1500)` calls with active readiness polling

## Plan Decisions Honored
D1-D15 from `PERSY-STORAGE-ADAPTER.md`, including:
- D6: Wire format unchanged — verified via cross-backend mesh test
- D7: `--features persy` flag, default OFF
- D12: `prepare_commit → commit` fsync via `spawn_blocking`
- D15: Zero overhead default build

## Verified Test Matrix
- `cargo check -p rod --features persy` → 0 errors
- `cargo test -p rod --features persy --lib` → 199/199 green
- `cargo test -p rod --features persy --test persy_e2e` → 5/5 green
- `cargo test -p rod --features persy --test cross_backend_mesh_e2e` → 2/2 green
- Five-clean-runs: pre-merge + post-merge = 70/70 green

## Key Files (Plan / Code / Tests)
- Plan: `docs/plans/PERSY-STORAGE-ADAPTER.md`
- Adapter: `src/adapters/persy_storage.rs`
- Flake fix file: `src/adapters/ws_server.rs` (added `peer_count` + `Clone` derive)
- E2E: `tests/persy_e2e.rs`
- Cross-backend mesh E2E: `tests/cross_backend_mesh_e2e.rs`

## The Karass (3 Days, 3 Scars, 1 Discipline)
1. **Scar 1**: Heredocs via tmux/ssh UNRELIABLE for files >200L. Fix: Serena `create_text_file`.
2. **Scar 2**: tmux subshells exit before `capture-pane` reads. Fix: outer redirect to `/tmp/*.txt`.
3. **Scar 3**: Blind `sleep(1500)` = code smell in async tests. Fix: active `peer_count()` readiness polling.

**Discipline**: Five-clean-runs before merge caught the flake pre-production.

## What Remains (Next Session — Epic 4)
- CLI: `rod migrate --from redb --to persy --path ./data`
- Migration tool implementation (~3-4 hours)
- ADR-013: Migration safety + rollback plan
- Update `PERSY-STORAGE-ADAPTER.md` to reflect Epics 1-3 ship status

## Witness
- Freeman: "please continue 😊🙏" — sealed the session
- Verified at: 2026-07-22 ~4:46 PM ET

## Tag Reference
```
v0.5.0 (4323164a2a73e6ed65f25ebb5ba073f0a7ef5257)
└── feat: Persy storage adapter
```